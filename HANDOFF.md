# 작업 인계 (2026-07-07 기준)

다음 Claude 세션이 이어서 작업할 때 참고할 현재 상태/맥락 요약.

## 프로젝트 목표

JLPT 한자(상용한자 2138자) 학습 PWA. **궁극적 목표는 "학습 플랫폼"** — 학습·퀴즈·개인 단어장(오답노트)이 하나로 이어지는 흐름이어야 함. 화면을 따로따로 만드는 게 아니라 이 통합 흐름을 항상 염두에 둘 것.

또 하나의 원칙: **AI가 데이터를 지어내지 않고, 공식 데이터셋(KANJIDIC2, JMdict) 기반으로 `data/*.json`을 자동 생성**한다. 확실하지 않은 값은 `xxxSource` 필드로 출처를 명시해서 신뢰도를 구분해둔다 (`kunKrSource`, `meaningKrSource` 참고).

## 오늘 한 작업

- **데이터 파이프라인**: `data-source/japanese/kanjidic2.xml`·`JMdict.xml`에서 파싱 스크립트(`scripts/parse-*.mjs`) → 캐시(`data/cache/*.json`) → diff 확인 → `apply-*.mjs`로 반영, 순서로 진행.
  - `onJp`(음독) 166/2138자를 kanjidic2 값으로 교체.
  - 부수(`data/radicals.json`) 훈음 92자 중 91자를 ko.wiktionary.org로 교차검증(34자 교체).
  - 부수마다 `level`(N5~N1) 필드 추가 — "이 부수가 쓰인 한자 중 가장 낮은 급수"로 역산.
  - `exampleKanji`/`exampleJp` 없던 1340자 중 1337자를 JMdict에서 채움. **`exampleKr`(한국어 뜻)은 여전히 비어있음** — JMdict엔 영어 뜻만 있고, 번역해서 채우면 "AI가 데이터 생성 안 함" 원칙 위반이라 사람이 나중에 채워야 함.
  - `kunJp`(훈독)는 의도적으로 안 건드림 — kanjidic2엔 훈독이 다 나열되는데, 기존 값은 학습용으로 사람이 추려낸 거라 그대로 교체하면 품질이 떨어짐.
  - `radicalNumber` 중 来·書·南 3건은 kanjidic2와 다르지만, 이미 japandict.com으로 직접 검증된 값이라 안 건드림 (kanjidic2 쪽이 다른 관례).
- **구조 개편**: 사이드바에 "일본어" 하위메뉴(학습/퀴즈/오답노트/부수) 추가, 퀴즈 설정에서 "전체(2138자)"·구간(1~8) 선택 제거.
- **부수를 학습 대상으로 편입**: `RadicalsPage`가 이제 학습 화면(급수 선택→배치 학습→진도 저장) 구조. 기존 획수별 전체 목록은 "전체 목록 보기" 토글로 남김.
- **퀴즈 이어하기/기록**: 퀴즈 완료 시 점수를 기록(홈에 최근 기록 리스트), 미완료 퀴즈는 문제/선택지/답까지 그대로 저장해서 정확히 이어서 풀 수 있음(홈의 "마무리못한 퀴즈" 카드). 홈에 "학습 진도" 카드도 추가.
- **버그 수정**: 퀴즈 정답 상태가 다음 문제로 새는 문제, 학습 이전/다음 버튼 위치가 콘텐츠 길이에 따라 움직이던 문제, 퀴즈/결과/학습완료 화면에서 키보드 포커스가 계속 사라지던 문제(전부 수정 완료).
- **백업 커버리지**: `studyProgress`/`radicalStudyProgress`/`inProgressQuiz`가 백업 export/import에 빠져있던 걸 추가 (v4).

## 아직 안 한 것 / 다음에 볼 것

1. **학습 콘텐츠가 N5(80자)밖에 없음** — `data/study-n5.json`만 존재. 전체 2138자 중 2058자는 학습 화면에 유래(etymology)/부수 카드가 없음. N4~N1용 `data/study-{level}.json`을 만들어야 하는데, `etymology`는 모델이 직접 써야 하는 필드라(교차검증 불가) 급수 내려갈수록 신뢰도 낮아짐 — README(`data/raw/README.md`) 참고.
2. **`exampleKr` 1337건** — JMdict로 채운 단어들의 한국어 뜻이 비어있음. 사람이 채우거나, 별도 방법(사전 API 등) 필요.
3. **JMdict.xml은 git에 안 올라감** — 114MB로 GitHub 100MB 제한 초과. `.gitignore`에 등록돼 있고 로컬에만 있음. 필요하면 재다운로드하거나 사용자가 직접 관리.

## 작업 방식 관련 참고

- 매 작업 단위마다 확인 없이 바로 commit+push (momogirin 계정).
- git 관련 위험한 작업(rebase, reset --hard 등)은 자동 분류기가 막으므로 사용자에게 먼저 물어볼 것.
- Edit 후 `tsc`/`lint`/`build`를 스스로 돌리지 말 것(사용자가 명시적으로 싫어함) — 단, 이번 세션엔 복잡한 리팩터링 검증 목적으로 몇 번 어겼음, 주의.
- 데이터 스크립트는 항상 "파싱→캐시→diff 확인→적용" 순서로, 한 번에 밀어붙이지 말 것.
