# 일본어 학습 기능 로드맵

이 파일은 **일본어(JLPT) 학습 기능의 앞으로 구현 방향**을 지속적으로 추적하는 문서다.
(영어/TOEIC은 2026-07-22부터 보류 상태이므로 이 문서 범위 밖 — HANDOFF.md 참고.)

- HANDOFF.md = "지금까지 무엇을 했는가"(세션 인수인계 스냅샷)
- **ROADMAP.md(이 파일) = "앞으로 무엇을 할 것인가"**(항목별 상태를 계속 갱신)

항목을 완료하면 상태를 `완료`로 바꾸고 완료일·커밋 요약을 한 줄 남긴다.
새 항목이 생기면 아래 우선순위 표에 추가하고, 판단 근거도 같이 적는다.

최초 작성: 2026-07-22 (앱 전체를 일본어 학습 관점에서 점검한 결과)

---

## 작업 진행 원칙 (2026-07-22 추가)

이 로드맵의 각 항목을 실제로 구현할 때 지킬 것:

- **병렬작업(서브에이전트 동시 실행)은 사용자가 명시적으로 "병렬로 해"라고 지시할 때만 쓴다.**
  기본은 순차 작업이다. 이유: 과거 병렬 서브에이전트를 여러 개 동시에 띄우다 사용량(usage)을
  너무 빨리 소진해서, 완료 전이던 작업 결과물이 통째로 폐기된 사고가 반복됐다.
- **항상 기능/파일 단위로 작업을 끊고, 그 단위가 끝나면 바로 커밋한다.**
  단, 한 줄 수정마다 커밋하는 식으로 너무 잘게 쪼개지는 않는다 — "의미 있게 완결된 단위"가 기준.
  (예: "문법 빈칸 퀴즈 생성기 구현" 완료 시점에 커밋, "화면 연동" 완료 시점에 또 커밋 — 이 정도 단위)
- **병렬작업을 허가받은 경우에도** 사용량 소진으로 중단될 수 있음을 항상 염두에 두고,
  한 번에 너무 많은 서브에이전트/청크를 동시에 띄우지 않는다. 적정량 작업 → 커밋 → 다음 배치
  리듬을 유지해서, 중단되더라도 이미 끝난 부분은 커밋으로 안전하게 남긴다.
  (대량 데이터 생성 작업의 청크 크기 기준은 HANDOFF.md의 "서브에이전트 배치 크기" 관련 기록 참고.)
- **사용량이 부족해 보이면 큰 단위작업을 시작하기 전에 먼저 알린다.** 끝까지 완료할 수 있을지
  불확실한 상태에서 큰 작업에 들어가지 말고, 더 작은 단위로 쪼개거나 사용자에게 미리 상황을
  공유한 뒤 진행한다.
- **모델은 기본적으로 사용자가 지정한 대로 고정한다(현재 Sonnet).** 다만 6번(품사 태깅)처럼
  언어학적 판단 오류가 데이터에 영구히 박힐 수 있는 고위험 항목에 한해서는, 착수 시점에
  "이 항목은 Opus 전환을 권장한다"고 먼저 알리는 것을 예외로 허용한다 — 매번 새로 허락받을
  필요 없이, 이 문서에 근거해 바로 제안한다.

---

## 판단 기준

이 프로젝트의 1순위 기준은 **"UI를 예쁘게 다듬는 것보다 실제로 공부에 도움이 되는가"**다.
따라서 로드맵 우선순위도 아래 순으로 매긴다.

1. JLPT 실제 출제 형식과의 정합성 (시험에 나오는 능력을 실제로 훈련시키는가)
2. 실사용 일본어 능력 (읽기/쓰기/활용 등 산출 능력)
3. 신규 데이터 없이 기존 데이터만으로 되는가 (투입 대비 효과)
4. UI/편의

---

## 현재 진단 요약 (2026-07-22)

핵심 콘텐츠(한자 2138자·단어 7972개·문법 470개·가나)는 전부 채워져 있으나,
**그 데이터를 쓰는 "문제 유형"이 빈약해서 학습효과가 데이터 규모를 못 따라간다.**

- 단어 퀴즈: "뜻 4지선다" **1종뿐** (`vocabQuizGenerator.ts`)
- 문법 퀴즈: "문형 → 뜻 고르기" **1종뿐** (`grammarQuizGenerator.ts`)
- 한자 퀴즈: 6종(훈독/음독/역방향 등) — 세 도메인 중 유일하게 다양함

JLPT 언어지식 파트 대문제 대비 현황:

| JLPT 대문제 | 현재 대응 |
|---|---|
| 漢字読み (한자 읽기) | 한자 퀴즈로 부분 대응 |
| 表記 (히라가나 → 한자 표기) | **없음** |
| 文脈規定 (문맥에 맞는 어휘 고르기) | **없음** |
| 言い換え類義 (유의어 바꿔쓰기) | **없음** |
| 用法 (그 단어의 올바른 쓰임) | **없음** |
| 文法1 (빈칸에 맞는 문형) | **없음** — 뜻 고르기만 있음 |
| 文の組み立て (문장 배열, ★칸) | **없음** |

또한 **동사 활용(活用) 훈련이 전무**하다 — `vocab-*.json`에 품사(`pos`) 필드 자체가 없어서
5단/1단/불규칙 구분이 데이터에 없고, て형·ない형·사역·수동·가능형 드릴을 만들 수 없다.
N4~N3 학습자가 가장 많이 막히는 지점이 정확히 여기다.

참고: 영어(TOEIC) 쪽엔 이미 `exampleEn`에서 단어를 `_____`로 가리는 빈칸형 퀴즈
(`generateEnglishVocabDerivationQuestions`)가 구현돼 있는데, **정작 일본어엔 같은 패턴이 없다.**
1~3번 항목은 그 패턴을 그대로 미러링하면 되므로 신규 데이터 없이 구현 가능하다.

---

## 우선순위 표

| # | 항목 | 신규 데이터 | 학습효과 | 상태 |
|---|---|---|---|---|
| 1 | 문법 빈칸 퀴즈 (문형 마스킹) | 소량(수작업 판단) | 매우 큼 | 완료 (2026-07-22) |
| 2 | 단어 문맥 빈칸 퀴즈 | 없음 | 큼 | 완료 (2026-07-22) |
| 3 | 단어 읽기 입력 퀴즈 (산출 훈련) | 없음 | 큼 | 완료 (2026-07-22) |
| 4 | 한자 표기 퀴즈 (히라가나 → 한자) | 없음 | 큼 | 완료 (2026-07-22) |
| 5 | 학습 카드 자가평가 → SRS 편입 | 없음 | 큼 | 완료 (2026-07-22) |
| 6 | 동사/형용사 활용 드릴 | vocab 1281개 태깅 | 매우 큼 | 완료 (2026-07-22, SRS 편입 2026-07-23) — 동사·い·な형용사 전 급수 태깅 + 드릴 확장 + SRS |
| 7 | 자타동사 짝 연결 | 짝 ID만 | 중간 | 완료 (2026-07-22) |
| 8 | 한자 → 단어 역인덱스 / 음독별 예문 | 0 | 중간 | 완료 (2026-07-22) — A·B 모두 완료 |
| 9 | 가나 최소대립쌍(촉음·장음) 퀴즈 | 소량 | 중간 | 완료 (2026-07-22) |
| 10 | 진도 표시를 SRS 정착도 기준으로 | 없음 | 중간 | 완료 (2026-07-22, 병기 방식) |
| 11 | 가나 퀴즈 오답노트/SRS 연동 | 없음 | 중간 | 완료 (2026-07-22 SRS, 2026-07-23 오답노트·홈 연동) |
| 12 | 모의고사에 실전 문제유형 편입 | 없음 | 매우 큼 | 완료 (2026-07-23) |
| 13 | 활용 드릴 오답노트 편입 | 없음 | 중간 | 완료 (2026-07-24) |
| 14 | 유의어(言い換え類義)·용법(用法)·문장배열(文の組み立て) | 일부 필요 | 큼 | 유의어·용법 완료(2026-07-24), 문장배열 미착수(형태소 분석기 필요) |

1~5는 전부 **기존 데이터만으로** 가능하고 기존 패턴 미러링 수준이라 투입 대비 효과가 가장 좋다.
6번은 비용이 크지만 일본어 학습앱으로서 가장 큰 공백이다.

---

## 항목별 상세

### 1. 문법 빈칸 퀴즈 — 상태: 완료 (2026-07-22)

**문제**: 현재 문법 퀴즈는 "문형 → 뜻 고르기"뿐이다. 「あげく」의 *뜻*을 아는 것과,
문장 빈칸에 「あげく / すえに / 末に」 중 무엇을 넣을지 아는 것은 다른 능력이고,
**시험에 나오는 건 후자**다. 뜻 고르기만 반복하면 실제 문법 운용력이 안 늘어난다.

**당초 설계(폐기)**: `exampleJp`에서 `pattern`을 정규식으로 찾아 자동 마스킹하려 했으나,
착수 전 매칭률을 스크립트로 재본 결과 **전체 21%(N4는 0%, N1은 2%)로 실사용 불가 수준**
이었음 — `pattern`이 조사 골격(「～は～です」)이거나 활용형 카테고리명(「可能形（話せる・
食べられる）」)이라 예문에 그 문자열 그대로 안 나오는 경우가 대부분이었다.

**실제 채택한 설계**: `grammar-*.json`에 `blankJp`(빈칸 문장, `_____` 포함)와
`blankAnswer`(정답 표현) 필드를 새로 추가 — 자동 마스킹이 아니라 **항목마다 어느 부분을
가릴지 직접 판단**해서 채운다. 어미/활용 부분만 최소 단위로 가리고(문장 전체 마스킹 금지),
문장 전체가 그 문형 자체라 도저히 부분만 못 가리는 극소수 항목은 두 필드를 생략(optional).
`GrammarBlankQuestion`(오답은 같은 급수 내 `blankAnswer`가 겹치지 않는 다른 항목에서 추출)
을 `grammarQuizGenerator.ts`에 추가, `GrammarPage.tsx`에 "퀴즈 종류"(뜻 맞히기/문장 빈칸
채우기) 선택기로 연동 — `EnglishVocabPage`의 뜻맞히기/품사변환 퀴즈 종류 선택기와 동일 패턴.

**진행 방식**: 470개를 급수별 5개 서브에이전트에 병렬 위임(사용자가 이번 항목 한정으로
병렬·사용량 소진 허용 지시). 각 에이전트가 완료 후 자체 검증(`_____` 정확히 1회,
`blankJp`의 `_____`를 `blankAnswer`로 치환하면 원본 `exampleJp`와 100% 일치)까지 수행.

**완료 결과(2026-07-22)**: 470개 중 468개에 `blankJp`/`blankAnswer` 반영, 결측/중복 0건
확인(재구성 시 원본 `exampleJp`와 100% 일치, 스크립트 재검증 완료).
- N5: 85개 중 84개(1개 생략 — 「～から～まで」처럼 조사 두 개가 짝을 이루는 문형이라
  빈칸 1개로 표현 불가)
- N4: 59개 중 58개(1개 생략 — 「～し、～し」가 예문에 두 번 반복 등장해 단일 구간으로 못 가림)
- N3: 71개 전부(생략 없음)
- N2: 125개 전부(생략 없음)
- N1: 130개 전부(생략 없음 — 짧고 문형에 정확히 붙여 쓴 예문이 많았음)
- 코드 쪽(`grammarQuizGenerator.ts`/`storage.ts`/`GrammarPoint` 타입/`GrammarPage.tsx`) 전부
  완료, `npm run data:build:grammar` + `tsc -b --noEmit` 통과 확인.

**영향 파일**: `data/grammar-n1~n5.json`, `scripts/build-grammar-data.mjs`(`GrammarPoint`에
`blankJp?`/`blankAnswer?` 추가), `src/lib/grammarQuizGenerator.ts`, `src/lib/storage.ts`
(`GrammarBlankInProgressQuiz` 타입/CRUD, `BackupPayload` 14→15), `src/pages/GrammarPage.tsx`.

---

### 2. 단어 문맥 빈칸 퀴즈 — 상태: 완료 (2026-07-22)

**문제**: JLPT 「文脈規定」 유형(문맥에 맞는 어휘 고르기)이 없다.
뜻 4지선다는 단어→뜻 단방향만 훈련시키고, 실제 독해에서 필요한
"이 문맥에 어떤 단어가 오는가"를 훈련시키지 못한다.

**착수 전 매칭률 확인**: 원형 그대로 매칭 시 80%(7972개 중 6412개) — 문법(21%)과 달리
자동 마스킹만으로도 충분히 높았다. 나머지 20%는 예상대로 활용형 등장(「冷ます」→「冷まして」)
때문이었음. 어간 매칭(끝 1~2글자를 순서대로 제거하며 재시도, 단 어간이 2글자 미만으로
줄면 다른 단어와 오탐될 위험이 커서 포기)을 추가하니 96%(정적 스크립트 기준)까지 상승,
런타임 로직 그대로는 90%(7151/7972).

**채택한 설계**: 문법과 달리 **런타임 자동 마스킹**으로 충분해 수작업 데이터 추가가
필요 없었다. `vocabQuizGenerator.ts`에 `blankSentence()` 헬퍼(원형 매칭 → 어간 매칭 순서로
시도, 둘 다 실패하면 `null` 반환해 그 항목은 조용히 출제 풀에서 제외 — 추측해서 자르지
않는다는 원칙, `generateEnglishVocabDerivationQuestions`의 `blankSentence`와 동일 정신).
`word`가 세미콜론으로 이표기를 나열하는 경우(예: "足; 脚")는 첫 표기만 사용.
오답 선택지는 뜻 맞히기와 동일하게 같은 급수 풀에서 `meaningKr`가 겹치지 않는 단어로 추출.
`VocabPage.tsx`에 "퀴즈 종류"(뜻 맞히기/문맥 빈칸 채우기) 선택기로 연동 — `GrammarPage`의
동일 선택기와 같은 패턴. `storage.ts`에 `VocabBlankInProgressQuiz` 이어하기 상태 추가,
`BackupPayload` 15→16.

**영향 파일**: `src/lib/vocabQuizGenerator.ts`, `src/lib/storage.ts`, `src/pages/VocabPage.tsx`.
`npm run data:build:vocab` 불필요(데이터 변경 없음, 코드만 추가), `tsc -b --noEmit` 통과 확인.

---

### 3. 단어 읽기 입력 퀴즈 — 상태: 완료 (2026-07-22)

**문제**: 한자 퀴즈만 "훈음 입력"이 있고 나머지는 전부 4지선다다.
4지선다는 재인(recognition)만 측정해서 **실제 기억 강도보다 점수를 과대평가**시킨다
→ SRS 박스 4에 도달해도 실제로는 못 쓰는 단어가 쌓인다.

**확정한 처리**: 오쿠리가나는 전체 읽기 입력으로 확정(데이터의 `reading` 필드 형태 그대로,
예: 「冷ます」 정답은 「さます」 전체). 착수 전 확인 결과 vocab 7972개 전부 `reading`이
채워져 있어(빈 문자열 0건) 별도 제외 로직 없이 전량 출제 가능.

**구현**: `vocabQuizGenerator.ts`에 `vocabReadingLevelPool`/`generateVocabReadingQuestions`
추가(4지선다가 아니라 `VocabWord[]`만 반환 — 선택지 개념이 없음). 채점은 기존
`src/lib/answerMatching.ts`의 `isCorrectAnswer`(공백 무시 비교) 그대로 재사용.
`VocabPage.tsx`에 한자 `QuizRunner`의 `promptToAnswer`(텍스트 입력) 모드를 미러링한
`readingQuiz`/`readingQuizResult` phase 추가, "퀴즈 종류" 선택기에 3번째 옵션으로 연동.
`storage.ts`에 `VocabReadingInProgressQuiz` 이어하기 상태 추가(`BackupPayload` 16→17).

**영향 파일**: `src/lib/vocabQuizGenerator.ts`, `src/lib/storage.ts`, `src/pages/VocabPage.tsx`.
`tsc -b --noEmit` 통과 확인.

---

### 4. 한자 표기 퀴즈 (히라가나 → 한자) — 상태: 완료 (2026-07-22)

**문제**: JLPT 「表記」 대문제가 대응 없음. 읽기→표기 방향은 실제 쓰기 능력에 가깝다.

**착수 전 확인**: `word`가 순수 히라가나라 `word === reading`인 항목(표기 개념이 성립하지
않음)을 뺀 실제 출제 가능 규모는 7972개 중 6804개(85%) — 급수별로도 544~2319개씩 충분.

**구현**: `vocabQuizGenerator.ts`에 `vocabWritingLevelPool`/`generateVocabWritingQuestions`
추가. `reading`을 프롬프트로 주고 `word`를 4지선다로 고르는 구조. 오답 선택 로직
(`pickDistractors`)은 무작위 대신 우선순위를 둠 — ①읽기 첫 글자가 같은 단어(음이 비슷해
헷갈리기 쉬움) ②한자를 하나라도 공유하는 단어(자주 혼동하는 유사 한자어) ③그래도 부족하면
나머지 무작위, 순서대로 채워 3개를 확보. 착수 전 스크립트로 전 급수(544~2319개) 전량 검증한
결과 오답 3개 미만으로 부족한 항목 0건.
`VocabPage.tsx`에 "퀴즈 종류" 선택기 4번째 옵션(표기 고르기)으로 연동, 화면 구조는 1~3번과
동일한 병렬 패턴(이어하기/결과화면/`VocabWritingInProgressQuiz`). `BackupPayload` 17→18.

**영향 파일**: `src/lib/vocabQuizGenerator.ts`, `src/lib/storage.ts`, `src/pages/VocabPage.tsx`.
`tsc -b --noEmit` 통과 확인.

---

### 5. 학습 카드 자가평가 → SRS 편입 — 상태: 완료 (2026-07-22)

**문제**: `recordSrsReview`가 **퀴즈 채점 시에만** 호출된다(설계상 의도된 것이었음).
결과적으로 플래시카드로 아무리 봐도 SRS에 안 잡히고, SRS 대상은 "퀴즈에 나온 적 있는
항목"으로 한정된다 → **7972 단어 중 대다수가 SRS 큐에 진입조차 못 한다.**

**확정한 설계(오염 방지)**: `recordSrsReview`(퀴즈 채점 전용)와 별개로 `recordSrsSelfCheck`
함수를 신설 — "모르겠음"은 퀴즈 오답과 동일하게 박스 0으로 리셋하되, "알겠음"은 **박스를
최대 1까지만** 올린다(퀴즈 정답처럼 4까지 올라가지 않음). 이렇게 통계 오염을 막은 이유:
- `statsSummary.ts`의 `getDomainAccuracies()`/`getWeeklyStats()`(정답률 통계)는 SRS
  state가 아니라 **퀴즈기록(QuizHistoryEntry류)만** 읽으므로애초에 자가평가 영향을 안 받음.
- `getSrsMastery()`(정착도, 박스4=마스터 판정)는 SRS state를 직접 읽으므로, 자가평가만으로
  박스4까지 올라가 "마스터함"으로 잘못 집계되는 걸 막기 위해 최대 1로 제한.

**구현**: 한자(`StudyPage.tsx`)/단어(`VocabPage.tsx`)/문법(`GrammarPage.tsx`) 세 학습 카드
전부 "다음" 버튼을 "모르겠음"/"알겠음" 2버튼으로 교체 — 판정 자체가 다음 카드 진행을 겸함
(마지막 카드에서는 그대로 학습 완료로 이어짐). 키보드도 Enter = 알겠음 처리 후 다음,
←/→ = 순수 카드 이동(자가평가 없이 건너뜀)으로 분리. 색상은 기존 `--color-correct`/
`--color-incorrect` 토큰 재사용(신규 hex 없음), `StudyPage.css`를 세 페이지가 전부
import하므로 CSS는 한 곳에만 추가.

**영향 파일**: `src/lib/storage.ts`(`recordSrsSelfCheck`), `src/pages/StudyPage.tsx`,
`src/pages/VocabPage.tsx`, `src/pages/GrammarPage.tsx`, `src/pages/StudyPage.css`.
`tsc -b --noEmit` 통과 확인.

---

### 6. 동사/형용사 활용 드릴 — 상태: 드릴 완료 (2026-07-22), 전체 vocab pos 태깅은 미착수

**완료된 것(2026-07-22, 독립 데이터셋 방식)**: 로드맵 원안(vocab 7972개에 `pos`
태깅)과 달리, **활용 드릴 자체를 먼저 독립 데이터셋으로 구현**했다 — `data/conjugation.json`
(엄선 N5/N4 상용 동사 58 + 형용사 34 = 92개, 실단어에 활용유형 태깅, 5단/1단 분류는
표준사전 기준 검증: 帰る·入る·走る=五段, 着る=一段, きれい·嫌い=な형용사). 활용형은
`src/lib/conjugation.ts` 규칙 엔진이 런타임에 결정론적으로 생성(五段 音便·ない형 특례·
行く 예외·する/来る 불규칙·いい 불규칙, 검증 세트 통과). `ConjugationPage`(학습 활용표 +
목표 활용형 4지선다 퀴즈)를 어휘·문법 소분류에 '활용'로 추가. 이로써 N4~N3 학습자가 가장
막히는 활용 훈련의 핵심(ます·て·ない·た형 + 형용사)을 신규 vocab 태깅 없이 확보.
커밋: 가나 메뉴개편(1532178) 후속으로 활용 기능 추가.

**전체 vocab 태깅 완료(2026-07-22)**: `vocab-*.json`에 `conjugationType?`(godan/ichidan/
suru/kuru/iadj/naadj) 필드를 추가하고, **N5~N1 동사·い형용사 1069개**(五段 571·一段 289·
い형 186·する 22·くる 1)를 태깅했다. 방식: `meaningKr`이 "다"로 끝나는 술어 신호 +
어미(う단/い)로 후보를 뽑는 휴리스틱 → **5단/1단은 검증 예외목록으로 분류 후 급수마다
사람이 전수 검토**(동음이의 함정 다수 교정: 閉める·着る·寝る·変える·占める·代える=一段,
裏切る·横切る·区切る·締め切る·打ち切る 등 -切る 컴파운드=五段, 刷る·擦る=五段, 気に入る·
恐れ入る=五段, 振り返る·引っ繰り返る=五段). 인사말·표현·주석표기·불규칙 경어동사·문어읽기
(得る/うる)·세미콜론 복수표기는 제외. **な형용사는 어미 신호가 없어(元気·静か…) 이번
스코프에서 제외** — 별도 후속(사전/수작업 필요).

**드릴 확장 완료**: `src/lib/vocabConjugation.ts`로 태깅 어휘를 활용 엔진에 공급,
`ConjugationPage`의 학습·퀴즈에 "단어" 선택기(엄선/N5/N4/N3/N2/N1) 추가 → 이제 급수별
실제 어휘로 활용 연습 가능(10형 동사 + い형용사). 빈 풀 가드 포함.

**な형용사 태깅(2026-07-22 추가)**: な형용사는 meaningKr이 명사형("조용함·편리함")이라 "다"
신호로 안 잡히지만, **"함/움"으로 끝나는 미태깅 단어를 고정밀 신호로** 잡아 196개 태깅
(명사 오탐 欠陥·名刺·軍艦·명사화형 恐れ·慣れ 등은 제외목록으로 배제). 친절·위험처럼
"절/험"으로 끝나는 일부 な형용사는 놓쳤으나, な형용사 활용은 전부 동일(じゃない/だった/で/に)
이라 부분 커버로 충분. **총 1265개 태깅**(五段 571·一段 289·い형 186·な형 196·する 22·くる 1).
드릴은 conjugationType로 필터하므로 な형용사도 자동 포함됨.

**재검증(2026-07-23)**: "놓친 な형용사(친절·위험류) 보완"으로 남겨뒀던 항목을 다시
스크립트로 확인한 결과, 이미 후속 세션에서 해소돼 있었다(親切/危険 모두 태깅됨,
현재 な형용사 229개). meaningKr이 "함/움"으로 끝나는데 미태깅인 나머지 14개
(欠陥·名刺·戦い·慣れ·恐れ 등)는 실제로 な형용사가 아니라 명사/동명사형이라 원래
의도대로 정확히 제외된 것— 진짜 누락이 아님. exampleJp에서 "word+な/だ/です/に"
패턴으로 후보를 더 넓게 찾는 방법도 시도했으나 6691개 미태깅 단어 중 842개가
걸려 학생·의자·아파트 같은 명사 오탐이 압도적으로 많아 채택하지 않음(조사 に/だ가
명사 서술에도 그대로 붙기 때문에 규칙만으로는 구분 불가 — 언어학적 개별 판단
없이는 이 이상의 확장이 안전하지 않음).

**남은 것**: 활용 드릴을 오답노트/SRS에 편입(SRS는 2026-07-23 완료, 오답노트는
storage.ts 대수술이라 #11과 함께 후속 처리), 단어 화면에서의 품사 표시는 이미 배지로
구현됨(위 참고).

---

**(원안·참고)**

---

**(원안) 문제**: 일본어 학습앱의 가장 큰 공백. `vocab-*.json`에 `pos` 필드가 아예 없다
(영어 쪽엔 있는데 일본어엔 없음). 그래서 5단/1단/불규칙 구분이 없고,
て형·た형·ない형·사역·수동·가능·조건형 드릴을 만들 수 없으며, い/な형용사 구분도 없다.

「食べる」의 뜻을 아는 건 쉽지만 「食べさせられた」를 읽어내는 건 별개 능력이고,
현재 앱은 전자만 훈련시킨다. N4~N3에서 학습자가 가장 많이 막히는 지점이 여기다.

**설계**: `pos` 필드 추가 — `동사5단 / 동사1단 / する동사 / い형용사 / な형용사 / 명사 / 부사 / 기타`.
활용형은 규칙으로 자동 생성 가능하다(예외는 する·来る·行く 정도).

**비용**: 7972개 전량 태깅은 부담이 크다 → **동사·형용사만(대략 2천 개대)** 먼저 하면 충분하다.
5단/1단 구분은 어미만으로는 판정 불가능한 케이스가 있어(帰る=5단, 着る=1단)
기계 규칙만으로는 안 되고 검증이 필요하다.

**진행 방식**: 대량 생성 작업이므로 이 저장소 관례대로 청크 80~100개 / 병렬 4~5개
서브에이전트, 웨이브마다 즉시 커밋. 완료 보고를 텍스트로만 믿지 말고
**반드시 스크립트로 결측을 재검증**할 것(HANDOFF.md의 vocab 예문 보완 때 겪은 교훈).

---

### 7. 자타동사 짝 연결 — 상태: 완료 (2026-07-22)

上がる/上げる, 決まる/決める 같은 자타동사 짝은 JLPT 단골이고,
**데이터에 이미 두 단어가 다 들어 있다** → 짝 ID만 연결하면 새 퀴즈 유형이 하나 생긴다.
영어의 `wordFamilyId`와 같은 접근(공통 ID 필드 하나 추가).

**후보 추출**: 한자 어간이 같고 る로 끝나는 동사를 기계적으로 그룹핑(112개 어간 그룹,
266개 단어) → 순수 어미 패턴 매칭은 오탐이 많아(教える/教わる 같은 능동/수동 관계,
生きる/生じる/生る처럼 어간만 같은 무관한 동사 등) 서브에이전트가 언어학적으로 실제
자타동사 짝인지 직접 판단하도록 위임.

**결과**: 266개 후보 중 **65세트(133개 단어)**를 진짜 자타동사 짝으로 확정, 나머지는
오탐으로 제외. 세트 크기는 2개짜리 62세트, 3개짜리 3세트(混ざる/混じる/混ぜる 등).
급수별 분포: N5 14·N4 23·N3 60·N2 15·N1 21개 단어. 검증(결측/중복 0건, 기존 필드
무손상 git diff 확인, 세트 목록 재검토) 완료.

**구현**: `vocab-*.json`에 `transitivityPairId`(짝의 공통 ID, 자동사 표제어를 그대로 씀)
필드 추가. `vocabQuizGenerator.ts`에 `VocabTransitivityQuestion` 생성기 추가 — 2번(문맥
빈칸 퀴즈)의 `blankSentence` 로직을 재사용하되, 오답 선택지가 무관한 단어가 아니라
**같은 짝의 다른 형태**라서(뜻은 비슷하고 자동사/타동사만 다름) 진짜 구분 능력을
테스트한다. `VocabPage.tsx`에 "퀴즈 종류" 5번째 옵션(자타동사 구분)으로 연동, `storage.ts`에
`VocabTransitivityInProgressQuiz` 이어하기 상태 추가(`BackupPayload` 18→19).

**영향 파일**: `data/vocab-n1~n5.json`, `scripts/build-vocab-data.mjs`, `src/lib/vocabQuizGenerator.ts`,
`src/lib/storage.ts`, `src/pages/VocabPage.tsx`. `npm run data:build:vocab` + `tsc -b --noEmit` 통과 확인.

---

### 8. 한자 → 단어 역인덱스 / 음독별 예문 — 상태: 완료 (2026-07-22, A·B 모두)

**A(역인덱스) 완료(2026-07-22)**: `src/lib/kanjiWordIndex.ts` 신설 — vocabList만으로
한자→단어 역인덱스를 lazy 구축(`wordsUsingKanji`/`wordsUsingKanjiCount`, 급수·번호순
정렬로 흔한 단어 우선, 기본 상한 12개). 한자 전체보기(KanjiListPage) 상세 카드에
"이 한자가 쓰인 단어 (n)" 섹션 추가, 기존 `study-used-kanji-chip` 스타일 재사용(신규
CSS·데이터 0건). tsc 통과. (한자 학습카드 StudyPage에도 같은 방식으로 확장 가능하나
StudyPage는 자가평가 등 다른 작업과 겹칠 수 있어 우선 전체보기에만 반영.)

**B(음독별 예문) 완료(2026-07-22)**: 원안은 수작업 데이터 추가를 전제했으나, 착수 전
자동 매칭 가능성을 스크립트로 검증한 결과 신규 데이터 없이도 충분한 커버리지가
나와 **런타임 자동 매칭**으로 방향을 바꿨다(2번 문맥 빈칸 퀴즈와 같은 판단).

매칭 방식 시행착오: ①단순 `includes` 46% — 하지만 大体(だいたい)가 タイ 음독에
오탐되는 등 부정확. ②word/reading 길이 완전 일치 요구 5.4% — 오쿠리가나 있는
단어 대부분이 걸러져 커버리지 붕괴. ③word를 한자런/오쿠리가나런으로 분리해 오쿠리가나를
reading에서 순서대로 찾아 한자런의 읽기를 역산하는 방식(熟語 중간/끝 글자는 위치를
정확히 못 잘라 매칭 포기) 채택 — 59.6%(589자 중 351자가 최소 1개 음독 예시 확보).
추가로 짧은 음독이 긴 음독의 접두여인 함정(分: ブン의 「ぶん」이 ブ의 「ぶ」로도
시작해 오매칭됨)을 발견해, 음독 후보를 긴 것부터 배정하고 이미 배정된 단어는
짧은 음독 매칭에서 제외하도록 수정.

**구현**: `src/lib/kanjiOnReadingIndex.ts` 신설(`onReadingExamples`). `KanjiListPage`
상세의 "일본 음독" 필드 안에 음독별로 대표 단어(최대 3개, 급수·번호순)를 인라인
표시, 매칭 안 되는 음독은 조용히 제외(추측 금지 원칙). CSS는 `study-used-kanji-chip`
재사용 + `study-on-reading-examples`/`study-on-reading-row`/`study-on-reading-label`
최소 추가. tsc 통과.

**영향 파일**: `src/lib/kanjiOnReadingIndex.ts`(신규), `src/pages/KanjiListPage.tsx`,
`src/pages/StudyPage.css`. 데이터 파일 변경 없음.

---

**(원안) 문제 A(역인덱스)**: `src/lib/kanjiUsage.ts`가 **단어 → 그 안에 쓰인 한자** 방향만 지원한다.
반대로 "이 한자가 쓰인 단어들"을 보는 화면이 없다. 데이터는 이미 다 있으므로
**인덱스만 만들면 되고 신규 데이터는 0건**이다. 한자 학습 카드/전체보기 상세에
"이 한자가 쓰인 단어" 목록을 붙이면 한자↔단어가 실제로 연결된다.

**문제 B(음독별 예문)**: `kanji.json`은 한 글자당 `exampleKanji` 1개다(日 → 毎日).
그런데 한자는 음독이 여럿이고(日: ニチ/ジツ), 학습자가 실제로 막히는 건
"이 단어에서 이 한자를 어떻게 읽는가"다. 음독/훈독별 대표 단어를 각각 붙이면
한자 → 독해력으로 이어진다. 다만 이건 데이터 추가가 필요하므로 A보다 뒤.

---

### 9. 가나 최소대립쌍(촉음·장음) 퀴즈 — 상태: 완료 (2026-07-22)

**완료(2026-07-22)**: `KanaQuiz`에 "유형" 선택기(가나↔로마자 / 표기 구분)를 추가하고,
러너를 정규화 문제(`NormQuestion`)로 리팩터링해 두 유형을 한 러너로 처리. 표기 구분은
`data/kana-pairs.json`(촉음 8·장음 8·요음 6 = 22개, 실단어+정확한 뜻, 오답 변형은 그 뜻에
대해 틀린 표기이며 일부는 뜻이 다른 실단어 함정: ビール↔ビル, おばあさん↔おばさん,
びょういん↔びよういん 등)에서 출제 — 뜻만 보고(로마자 노출 없이) 올바른 표기를 고른다.
`build-kana-pairs-data.mjs` + `data:build:kanaPairs`. 영향: `src/data/kanaPairs.ts`,
`src/pages/KanaPage.tsx`, `KanaPage.css`. tsc·build 통과.

---

**(원안)**

**배경**: TTS는 2026-07-20에 실사용 품질 문제로 전면 제거됨(그 판단은 유지).
그 결과 청해 대비가 0인데, 더 중요한 건 **일본어에서 의미를 가르는
촉음(きって/きて)·장음(おばさん/おばあさん)·요음 구분 훈련이 전혀 없다**는 점이다.

**현황 확인(2026-07-22)**: `KanaPage.tsx`에 표/학습/퀴즈 3탭이 이미 구현돼 있음.
다만 퀴즈는 **가나 ↔ 로마자 대응**(あ↔a)만 다뤄서, 정작 헷갈리는 함정
(촉음/장음/요음 구분)은 로마자 변환 문제로는 안 드러난다 — "가나 퀴즈가 없어서"가
아니라 "있는 퀴즈가 진짜 함정을 안 건드려서" 채워야 하는 항목.

**설계**: 소리 없이도 가능한 **최소대립쌍 표기 퀴즈** — 「ちょっと/ちょと」,
「ビール/ビル」처럼 헷갈리는 짝을 눈으로 구분하는 문제. 표기 정확도가 올라간다.
기존 `KanaQuiz`의 setup/running/result 구조를 그대로 재사용하고 문제 유형만 추가.

**범위 밖**: 진짜 청해는 브라우저 TTS가 아니라 녹음 오디오가 필요하고,
그건 이 로드맵과 별개 규모의 일이다(현재로선 계획 없음).

---

### 10. 진도 표시를 SRS 정착도 기준으로 — 상태: 완료 (2026-07-22, 병기 방식)

**완료(2026-07-22)**: 홈 화면 "학습 진도" 카드의 detail에 기존 "학습함(카드 넘김)
n/total"과 함께 **"정착 N개"**(SRS 박스4=30일 간격 도달 = 실제 외운 항목)를 병기.
`getSrsMastery(domain).mastered`를 4개 도메인 진도 카드에 각각 추가(신규 계산 없음,
기존 함수 재사용). 진도를 인덱스 기반에서 완전히 대체하는 대신 원안이 제시한
"최소한 병기" 옵션을 택함 — 카드 넘김과 실제 정착을 한눈에 대비.

---

**(원안) 문제**: `studyProgress.ts`가 인덱스 기반이라 "급수 완료" = *카드를 끝까지 넘겼다*는
뜻이지 외웠다는 뜻이 아니다. 실제 정착률은 SRS 쪽에만 따로 있어서,
홈 화면의 "N3 완료"가 학습자에게 정직하지 않은 신호를 준다.

**설계**: 진도 표시를 SRS 정착도 기준으로 바꾸거나, 최소한 병기.
5번(자가평가 → SRS 편입)이 먼저 되어야 의미가 있다.

---

### 11. 가나 퀴즈 오답노트/SRS 연동 — 상태: 완료 (2026-07-22 SRS, 2026-07-23 나머지)

**SRS 연동 완료(2026-07-22)**: `SrsDomain`에 `'kana'` 추가(+ `SRS_STATE_KEY.kana`,
statsSummary/HomePage의 타입 충족용 키 각 1개). 가나 **로마자 퀴즈**(가나↔로마자) 채점
시 `recordSrsReview('kana', 가나id, 정답여부)` 호출 → 틀린/복습주기 된 낱자가 SRS 큐에
편입. 가나 퀴즈 setup(로마자 유형) 상단에 **"복습할 가나 N자 — 지금 복습하기"** 배너를
띄워 `getDueSrsIds('kana', …)`로 뽑은 due 낱자만 로마자 퀴즈로 재도전. 표기 구분(pairs)
모드는 낱자 재인이 아니라 SRS 대상 아님.

**BackupPayload 편입 완료(2026-07-23)**: `srsKana`를 백업/클라우드 동기화 페이로드에
포함(version 20→21) — 활용 드릴 SRS 편입과 같은 커밋에서 storage.ts를 다시 손보면서
같이 처리(당시 우려했던 "병렬 세션과의 충돌"은 이 세션에서는 해당 없음).

**오답노트 UI 편입 완료(2026-07-23)**: `KanaWrongNoteEntry`/`addKanaWrongNotes`/
`removeKanaWrongNote`를 다른 도메인과 같은 패턴으로 추가. 로마자 퀴즈 결과 화면
진입 시 오답을 쌓고 맞힌 항목은 제거(표기 구분 모드는 SRS와 동일하게 제외).
`WrongNotePage`에 5번째 섹션(가나)+`KanaDetailCard` 추가 — "오답만 재도전"은 별도
retryIds 배선 없이 가나 페이지로 이동만 시키면 이미 있는 SRS due 배너가 자동으로
해당 낱자를 보여주는 방식이라 앱 상태 배선이 최소화됨. BackupPayload 21→22.

**홈 화면 연동 완료(2026-07-23)**: "지금 할 일"에 가나 복습 카드(SRS due), "오답
재도전" 그리드에 가나 카드 추가 — 다른 4개 도메인과 동일 패턴. "최근 기록" 병합
목록은 가나가 회차별 총점을 남기는 `SimpleQuizHistoryEntry` 자체가 없는 구조라
이번 스코프에서 제외(신설하려면 별도 히스토리 기록 도입이 필요한 확장 작업).

---

**(원안)**

**문제(2026-07-22 발견)**: `KanaPage.tsx`의 `KanaQuiz`는 채점을 로컬 `answers` state에만
쌓고, 한자/단어/문법 퀴즈와 달리 **오답노트(`addWrongNotes`류)·SRS(`recordSrsReview`)·
퀴즈기록(`SimpleQuizHistoryEntry`) 어디에도 연결돼 있지 않다.** 홈 화면 "최근 기록"·
"학습 통계"에도 가나 퀴즈 결과가 전혀 반영되지 않는다.

**설계**: 다른 세 도메인과 같은 패턴으로 연결 — 채점 시 오답을 오답노트에 쌓고,
정답/오답 여부를 SRS에 기록하고, 완료 시 퀴즈기록에 남긴다. 다만 가나는 `KanjiLevel`
축이 없어서(청음/탁음/요음 그룹 축) `SimpleQuizHistoryEntry.level` 타입을 그대로
못 쓸 수 있음 — 착수 시 타입 설계 확인 필요.

**우선순위 참고**: 학습효과보다 "다른 도메인과의 일관성" 문제에 가까워서 10개 항목 중
후순위. 다만 9번(최소대립쌍 퀴즈)과 작업이 겹치므로 같이 진행하면 효율적이다.

---

### 12. 모의고사에 실전 문제유형 편입 — 상태: 완료 (2026-07-23)

**문제(2026-07-23 발견)**: 1~11번으로 개별 연습 페이지에 실전 퀴즈 유형(문맥 빈칸·읽기
입력·표기·자타동사·문법 빈칸 등)을 대거 추가했는데, **정작 시험 시뮬레이션인 모의고사는
여전히 "뜻 고르기" 3종(한자→훈독뜻 / 단어→뜻 / 문법→뜻)만** 섞고 있었다. 로드맵 스스로
"4지선다 뜻 고르기는 재인만 측정해 점수를 과대평가시킨다"고 적어놓고, 정작 모의고사가
그 원시적 유형만 쓰고 있던 모순. JLPT 언어지식은 漢字読み·表記·文脈規定·文法이 섞여
나오는데, 그걸 흉내 낼 생성기가 이미 다 있는데도 모의고사에 연결이 안 돼 있었다.

**설계(최소 diff)**: `MockExamQuestion`이 `{domain, id, prompt, promptSub?, choices}`
균일 형태이고 페이지/스토리지/오답노트/SRS 배선이 전부 이 형태와 `domain`·`id`만 보고
돌아가므로, **`mockExamGenerator.ts` 내부에서만** 기존 퀴즈 생성기들(`quizGenerator`/
`vocabQuizGenerator`/`grammarQuizGenerator`)의 다양한 유형을 이 균일 형태로 매핑해
라운드로빈으로 섞도록 재구성. 페이지·`storage.ts`·`BackupPayload` 변경 0건, 실제 id·
domain을 유지하므로 오답노트·SRS 라우팅도 그대로.

- **한자**: 한자→훈독뜻(kunKr) / 한자→훈독읽기(kunJp) / 한자→음독읽기(onJp) — 漢字読み
- **단어**: 뜻 / 文脈規定(문맥 빈칸) / 表記(히라가나→한자) / 自他動詞
- **문법**: 뜻 / 文法(빈칸에 맞는 문형)
- `roundRobin()`으로 유형을 균등하게 번갈아 뽑고 같은 항목(id)은 dedupe, 어떤 유형의 풀이
  작아도(자타동사 등) 나머지로 자연히 채움 → "뜻 고르기 독점" 해소.
- **정답 누설 방지**: 表記·文法 빈칸은 선택지 라벨에 읽기/문형을 노출하지 않도록 분기.

**검증(2026-07-23)**: 전 급수(N5~N1)×문항수(10/30)에서 정답 정확히 1개·정답 라벨 유일
(submit()의 label 매칭 안전)·프롬프트 정답 누설 0건 확인. 유형 분포도 두 급수·두 문항수
모두 7개 하위유형이 고르게 분포(뜻 편중 없음). tsc 통과.

**영향 파일**: `src/lib/mockExamGenerator.ts`만.

---

## 변경 이력

- 2026-07-22: 최초 작성. 일본어 관점 전체 점검 후 10개 항목 도출.
- 2026-07-22: 가나 페이지 실제 확인 후 9번 상세에 현황 반영, 11번(가나 퀴즈 오답노트/SRS 미연동) 추가.
- 2026-07-22: 1번(문법 빈칸 퀴즈) 완료 — 470개 중 468개 데이터 반영 + 화면 연동까지.
- 2026-07-22: 2번(단어 문맥 빈칸 퀴즈) 완료 — 신규 데이터 없이 런타임 자동 마스킹(어간
  매칭 포함)만으로 구현, 매칭률 90%.
- 2026-07-22: 3번(단어 읽기 입력 퀴즈) 완료 — 오쿠리가나는 전체 읽기 입력으로 확정,
  한자 QuizRunner의 텍스트 입력 모드를 미러링.
- 2026-07-22: 4번(한자 표기 퀴즈) 완료 — 오답 선택지를 읽기 유사/한자 공유 우선순위로
  뽑아 변별력 확보, 전 급수 오답 부족 0건 검증.
- 2026-07-22: 5번(학습 카드 자가평가 → SRS 편입) 완료 — recordSrsSelfCheck 신설,
  박스 최대 1로 제한해 퀴즈 채점 기반 통계·정착도 오염 방지.
- 2026-07-22: 7번(자타동사 짝 연결) 완료 — 기계적 후보 추출 후 서브에이전트가
  언어학적으로 판단, 266개 후보 중 65세트(133개 단어) 확정.
- 2026-07-22: 가나(히라가나·가타카나) 학습 기능 신규 추가 + 일본어 메뉴를 학습단계별
  소분류(문자/어휘·문법)로 개편, 오답노트 최상위 승격(커밋 1532178).
- 2026-07-22: 6번(활용 드릴) 드릴 부분 완료 — 엄선 92개 독립 데이터셋 + 규칙 엔진 +
  ConjugationPage(활용표/퀴즈). 전체 vocab pos 태깅은 확장 과제로 남김.
- 2026-07-22: 6번 활용 엔진에 가능·의지·조건ば·사역·수동형 추가(동사 10형). 一段
  가능형=수동형 동형 문제는 퀴즈 선택지 중복 제거로 처리.
- 2026-07-22: 9번(가나 최소대립쌍 퀴즈) 완료 — 촉음·장음·요음 22쌍, 뜻→표기 고르기
  유형을 KanaQuiz에 추가(러너 정규화 리팩터링).
- 2026-07-22: 8번-A(한자→단어 역인덱스) 완료 — kanjiWordIndex.ts 신설, 한자 전체보기
  상세에 "이 한자가 쓰인 단어" 표시(신규 데이터 0건). B(음독별 예문)는 미착수.
- 2026-07-22: 6번 활용 퀴즈에 '활용형 읽기(형태 식별)' 모드 추가 — 활용형을 보고 무슨
  형인지 고르는 방향(독해력). 기존 produce 모드와 유형 선택기로 전환.
- 2026-07-22: 6번 활용 형용사 부사형(高く/静かに) 추가, 활용 학습·퀴즈에 그룹 필터
  (5단/1단/する·くる/い형/な형) 추가.
- 2026-07-22: 11번(가나 퀴즈 SRS 연동) 부분 완료 — SrsDomain에 kana 추가, 로마자 퀴즈
  채점을 SRS에 기록 + 가나 페이지 내 "복습할 가나 N자" 배너로 due 낱자 재도전. 백업
  포함·오답노트·홈 연동은 storage 대수술이라 보류.
- 2026-07-22: 활용 데이터 92→146 확장.
- 2026-07-22: 6번(전체 vocab 품사 태깅) 완료 — N5~N1 동사·い형용사 1069개에
  conjugationType 태깅(급수마다 사람이 5단/1단 함정 전수 검토), 활용 드릴에 급수별
  "단어" 선택기 추가로 전체 어휘 활용 연습 가능.
- 2026-07-22: 6번 な형용사 196개 추가(총 1265개) + 단어 카드에 활용유형 배지 표시. #6 완료.
- 2026-07-22: 10번(진도 SRS 정착도 병기) 완료 — 홈 진도 카드에 "정착 N개"(SRS 박스4) 병기.
- 2026-07-22: 8번-B(한자 음독별 예문) 완료 — 원안(수작업 데이터)과 달리 자동 매칭
  검증 결과가 충분해 런타임 로직으로 전환, 589자 중 351자 커버. #8 전체 완료.
- 2026-07-23: 6번(활용 드릴) SRS 편입 완료 — SrsDomain에 conjugation 추가, 퀴즈
  채점 시 기록 + "복습할 활용어 N개" 배너(KanaQuiz와 동일 패턴). BackupPayload
  19→20. "놓친 な형용사 보완" 재검증 결과 이미 해소돼 있어 작업 불필요 확인.
- 2026-07-23: 11번(가나 오답노트/SRS 연동) 나머지 전부 완료 — SRS BackupPayload
  편입(21), 오답노트 UI(WrongNotePage 5번째 섹션, BackupPayload 22), 홈 화면
  복습 카드·오답 재도전 카드 연동. #11 전체 완료.
- 2026-07-23: 12번(모의고사 실전 유형 편입) 완료 — 개별 페이지에만 있던 실전 퀴즈
  유형(漢字読み·表記·文脈規定·文法 빈칸·자타동사)을 mockExamGenerator 내부 라운드로빈
  으로 모의고사에 섞음. 페이지/스토리지 변경 0건. 13(활용 오답노트)·14(유의어/용법/
  문장배열)를 새 미착수 항목으로 등재.
- 2026-07-23: 한자 음독별 예시 커버리지 확장 — onReadingOverride 4개 패치(286자/327항목)를
  data/kanji.json에 병합, 음독 예시 확보 42%→54%. n1a(141자)는 KANJIDIC2 대조 결과
  전부 실재하나 表外 희귀음독이라(학습가치 낮음+상용어 부재) 보류.
- 2026-07-24: 유의어를 모의고사에도 편입(단어 5종 라운드로빈). 데이터 무결성 감사
  실행 — vocab/grammar/kanji 핵심 필드 결측 0, onJp는 KANJIDIC2와 100% 일치 확인
  (kunJp 756 "불일치"는 의도된 상용 큐레이션, radicalNumber 3건은 부수체계 차이라
  보류). n1a 음독 vocab 기계매칭은 오탐(短음독의 접두 함정+청탁 관용)이 많아 보류.
- 2026-07-24: 약점 기반 복습 강화 — statsSummary에 (도메인×급수)별 정답률 집계
  추가, 홈의 수동 "약점" 텍스트를 급수까지 콕 집고 클릭 시 해당 영역으로 이동하는
  실행형 카드로 교체. 표본 5문항 미만 급수는 우연 배제로 제외. 순수 코드, 신규 데이터 0.
- 2026-07-24: 14번 중 유의어(言い換え類義) 퀴즈 완료 — 신규 데이터 없이 기존
  meaningKr에서 유의어쌍 도출(같은 뜻+읽기/표기 다름, 이표기 제외). VocabPage에
  "유의어" 퀴즈 종류 추가(6번째), 오답노트·SRS·이어하기 연동, BackupPayload 23→24.
  실측 264+단어 출제 가능, 오탐률 ~5%(한국어 동음이의 電力/全力 등)는 규칙 분리
  불가라 코드 캐비어트로 명시. 용법(用法)·문장배열(文の組み立て)은 미착수(문장배열은
  형태소 분석기 필요). 정답이 entry가 아니라 answer인 점만 다른 퀴즈 유형.
  (SRS와 같은 활용 기본형 단위 id), 활용 퀴즈 결과 시 오답 적재·정답 제거,
  WrongNotePage 6번째 섹션(활용)+활용표 상세 카드(ConjTable 재사용), 재도전은 kana와
  동일하게 활용 페이지 이동→SRS due 배너. BackupPayload 22→23. #13 완료.
- 2026-07-24: 14번 중 용법(用法) 퀴즈 완료 — 신규 데이터 없이 구현. 文脈規定(2번)의
  역방향("이 단어 → 자연스러운 문장 고르기")으로, 정답은 entry 자신의 exampleJp를
  blankSentence로 가린 것, 오답은 뜻이 겹치지 않는(정답 2개 방지) 같은 급수 단어들의
  빈칸 예문. VocabPage에 "용법" 퀴즈 종류 추가(7번째), 오답노트·SRS·이어하기 연동,
  BackupPayload 26→27. 전 급수 출제풀 충분(N5 592·N4 562·N3 1961·N2 1480·N1 2556),
  1000문항 시뮬레이션 구조결함·뜻충돌 0 검증. 문장배열(文の組み立て)만 미착수로 남음
  (형태소 분석기 필요). (참고: BackupPayload 24→26은 별도 세션에서 가나 퀴즈 회차기록
  KanaQuizHistoryEntry를 최근기록·주간통계에 편입하며 올라간 것.)
