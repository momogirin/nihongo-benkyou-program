# 학습효과 로드맵 — "쓰면 진짜 는다"를 어떻게 보장·증명하나

작성: 2026-07-22. 렌즈: 학습 프로덕트 대표 + 마케터 + 기획자 —
단, 모든 판단 기준은 **매출이 아니라 "학습자가 실제로 실력이 늘고, 그걸 실감하는가"**다.

`ROADMAP.md`(문제유형 완성도)와 층이 다르다. 그쪽은 "어떤 문제를 낼까"이고,
이 문서는 **"낱개 문제를 다 풀어도 실력이 늘었다고 말할 수 있는가"**를 다룬다.

---

## 0. 한 줄 진단

> **지금 앱은 "낱개(item) 재인·재생 훈련기"로는 최상급인데,
> ①실사용 능력(독해·청해) ②효과의 증명 ③제때 복습 세 곳이 비어서,
> "많이 풀었다"는 되지만 "실력이 늘었다/시험에 붙는다"로 아직 다 연결되지 않는다.**

`ROADMAP.md`가 낱개 훈련(한자·단어·문법·활용 10종 퀴즈+SRS)을 이미 촘촘히 채웠다.
남은 병목은 **낱개 → 통합능력 → 성취실감**의 위쪽 사슬이다.

---

## 1. 학습효과 관점 5대 공백

### ① 능력축 공백 — 독해(读解)·청해(听解)가 훈련축에 없다 ★최대
- 근거: `mockExamGenerator.ts`의 `MockExamDomain = 'kanji' | 'vocab' | 'grammar'` —
  모의고사조차 **낱개 3도메인뿐**. 지문(passage) 읽고 푸는 독해가 없다. 청해는
  TTS 제거(2026-07-20) 이후 0.
- 왜 학습효과 문제인가: JLPT 점수의 절반 이상이 독해·청해이고, **실사용 능력의
  본체가 여기다.** 단어 7972개를 다 외워도 지문을 못 읽으면 "실력이 늘었다"가
  성립하지 않는다. 낱개 마스터리와 독해력은 다른 능력이다.

### ② 오답 피드백이 얕다 — "왜 틀렸는지"가 없다
- 근거: `QuizRunner.tsx`는 정/오답 색과 **정답 라벨만** 보여준다. 해설·예문
  재노출·오답 이유가 없다.
- 왜 문제인가: 학습과학에서 **정교화 피드백(elaborative feedback)** 이 단순
  정오 피드백보다 파지(retention)를 크게 올린다. 틀린 그 순간이 학습이 가장
  잘 박히는 지점인데 지금은 그냥 넘어간다.
- 좋은 점: 데이터에 이미 뜻·예문·읽기가 다 있다 → **신규 데이터 0으로**
  오답 순간에 붙일 수 있다. 투입 대비 효과 최상.

### ③ 효과가 학습자에게 안 보인다 — 증명 부재 (= 사용자가 말한 바로 그 니즈)
- 근거: `studyProgress`(카드 넘김)+`getSrsMastery`(박스4 정착)는 있으나,
  **"네 실력이 이만큼 늘었다"를 보여주는 지표가 없다** — 추정 급수, 모의고사
  점수 추이, 시간에 따른 정착 곡선.
- 왜 문제인가: 성취가 안 보이면 "이거 해서 느는 게 맞나?" 의심 → 이탈.
  **성취 실감이 지속학습의 근본 동력**이고, "실제로 도움이 된다"는 감각은
  진도바가 아니라 **능력이 오르는 게 눈에 보일 때** 생긴다.

### ④ 제때 복습을 못 만든다 — 간격반복이 새어나간다
- 근거: SRS 스케줄(`getDueSrsIds`)은 있으나 due가 됐을 때 학습자를 데려오는
  **트리거(알림)가 없다.** PWA인데 Notification 미사용.
- 왜 문제인가: 간격반복은 "정확한 타이밍에 다시 본다"가 전제다. 안 오면
  간격효과가 통째로 샌다. 이건 마케팅이 아니라 **학습 메커니즘의 미완성**이다.

### ⑤ 학습경로(커리큘럼)가 없다 — 도구모음이지 길이 아니다
- 근거: 홈에 취약 도메인 표시는 있으나(`getWeakestDomain`), "오늘 무엇을,
  얼마나, 어떤 순서로"를 앱이 처방하지 않는다. 평소 학습은 도메인·급수별로
  **블록화**돼 있고, 혼합연습(인터리빙)은 모의고사에만 있다.
- 왜 문제인가: 자기주도에 전부 맡기면 대부분 비효율적으로(쉬운 것만 반복,
  약한 것 회피) 공부한다. 또 학습과학상 **인터리빙(혼합)·바람직한 어려움**이
  블록연습보다 전이가 좋은데, 기본 흐름이 블록이다.

---

## 2. 로드맵 (학습효과 기준 우선순위)

우선순위 원칙: **신규 데이터 0이고 효과 큰 것 먼저 → 성취 증명 → 능력공백(독해) → 비용 큰 것(청해).**

### L0 — 효과를 "보이게" 만든다 (신규 데이터 0, 최우선)
- **성취 대시보드**: 추정 급수(정답률·정착도에서 산출), 모의고사 점수 추이,
  정착 항목 곡선. 기존 `statsSummary`/`getSrsMastery`/모의고사 기록 재사용.
  → 사용자가 요구한 "실제로 는다"를 **눈에 보이게** 하는 핵심 슬라이스.

### L1 — 낱개를 실력으로 (효과 대비 저비용)
- **오답 즉시 해설/예문**(신규 데이터 0) — ✅ **완료 (2026-07-22)**: 4개 도메인
  퀴즈 전부 오답 순간에 항목 카드(뜻·예문·해설)를 재노출. 한자(`QuizRunner`
  `KanjiHint`), 단어 5종(`VocabHint`), 문법 2종(`GrammarHint`, explanation 필드
  포함), 활용 2종(`ConjHint`). 공통 `.quiz-explanation` 스타일(QuizRunner.css)
  재사용, 신규 데이터 0. 모의고사는 시험 재현 특성상 제외(결과화면 해설 후속 검토).
- **SRS 제때 복습 리마인드**: due 쌓이면 알림. `getDueSrsIds`를 알림으로
  꺼내면 됨(방식은 DECISIONS.md #7 — 앱 내 배지/카드는 결정 없이 진행 가능).
  간격효과 완결.

### L2 — 통합·전이 (혼합연습)
- **"오늘의 학습" 처방**: SRS due + 취약 도메인 + 신규 진도를 **인터리빙으로
  자동 조립**한 하루치 세션. 자기주도 부담 제거 + 전이 좋은 순서 강제.
- **평소 학습 인터리빙 옵션**: 도메인/급수 혼합 모드(모의고사 로직 일부 재사용).

### L3 — 능력축 채우기 (신규 데이터 필요, 규모 큼)
- **독해 훈련**(★능력 최대 공백): 짧은 지문 + 내용/문법/어휘 문제. JLPT 독해
  대문제 형식 정합. 지문 저작이 필요해 규모가 크므로 급수 1~2개부터 파일럿.
- **청해**(가장 뒤): 브라우저 TTS가 아니라 녹음 오디오가 필요 → 이 로드맵과
  별개 규모. 당장 계획 없음(현실적 판단).

---

## 3. 지금 당장의 작은 것 (반나절~1일, 신규 데이터 0)

1. 오답 결과화면에 예문·뜻 재노출 1줄 추가 → 즉시 파지 상승.
2. 홈에 "추정 급수" 카드 1칸(정답률·정착도 기반 러프 추정)이라도 먼저.
3. 모의고사 결과에 이전 회차 대비 ±점수 표시(추이 실감).

> 실제 구현은 별건이며, 신규 데이터 저작(독해 지문 등)이 필요한 항목은
> `ROADMAP.md`의 작업원칙(청크·검증·커밋 단위)을 그대로 따른다.

---

## 변경 이력
- 2026-07-22: 최초 작성. 학습효과("실제로 는다") 관점에서 점검 → 5대 공백
  (독해·청해 / 오답해설 / 효과증명 / 제때복습 / 학습경로) 도출, 증명→저비용→
  능력공백 순 로드맵 수립.
- 2026-07-22: L1 "오답 즉시 해설" 완료 — 한자·단어(5종)·문법(2종)·활용(2종)
  전 도메인 오답 시 항목 카드 재노출. 결정 대기 항목(독해 범위·청해·알림 방식)은
  DECISIONS.md #5~#7에 기재.
